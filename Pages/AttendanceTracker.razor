@code {
    private List<string> attendedEvents = new();

    private void MarkAttendance(string eventName) {
        if (!attendedEvents.Contains(eventName)) {
            attendedEvents.Add(eventName);
        }
    }
}